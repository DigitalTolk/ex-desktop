# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
# Ignore dist *contents* but not the directory itself — keeping it
# tracked (via dist/.gitignore) means the //go:embed in frontend.go
# always has something to match on a fresh checkout.
dist/*
!dist/.gitignore
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
